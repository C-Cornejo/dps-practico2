import * as React from 'react';
import { View,Text } from 'react-native';

export default function Multiplicacion(props)
{
    const{navigation} = props;
    return(
        <View> 
            <Text>Multiplicacion</Text>
        </View>
    );
}